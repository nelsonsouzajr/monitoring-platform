apiVersion: 1

datasources:
  - name: Monitoring PostgreSQL
    type: postgres
    access: proxy
    url: db:5432
    database: monitoring
    user: monitor
    secureJsonData:
      password: monitor
    jsonData:
      sslmode: disable
      postgresVersion: 1600
      timeInterval: 30s
    isDefault: true
